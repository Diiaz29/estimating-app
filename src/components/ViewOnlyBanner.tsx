export default function ViewOnlyBanner() {
  return (
    <p className="rounded-lg border border-slate-300 bg-slate-100 px-4 py-2 font-mono text-[11px] uppercase tracking-wider text-slate-500">
      View only — your role can look at this page but changes won't save
    </p>
  )
}
